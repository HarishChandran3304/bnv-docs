---
title: bnv import
---

## Usage

```bash
bnv import ENV_FILE_PATH
```

## Description

Parse a `.env` file and import all variables into the encrypted store. Optionally delete the `.env` and create a `.better-env` file.

### Examples

```bash
bnv import ./.env
bnv import /abs/path/to/.env
```

